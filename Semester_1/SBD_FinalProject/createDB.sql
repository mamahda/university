-- Drop the 'cafe' database if it exists
DROP DATABASE IF EXISTS cafe;

-- Create the 'cafe' database if it does not exist
CREATE DATABASE IF NOT EXISTS cafe;

-- Select the 'cafe' database for use
USE cafe;